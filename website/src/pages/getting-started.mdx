import { Tab, Tabs, Cards, Card, Steps } from 'nextra-theme-docs';
import { toast } from '@codedbypol/logger';

# Getting Started

Logger is an opinionated toast component for React. You can read more about why and how it was built [here](https://polgubau.com/ui/building-a-toast-component).

<Steps>
### Install

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab>
    ```bash
    pnpm i @codedbypol/logger
    ```

  </Tab>
  <Tab>
    ```bash
    npm i @codedbypol/logger
    ```
  </Tab>
  <Tab>
    ```bash
    yarn add @codedbypol/logger
    ```
  </Tab>
    <Tab>
    ```bash
    bun add @codedbypol/logger
    ```
  </Tab>
</Tabs>

### Add Toaster to your app

It can be placed anywhere, even in server components such as `layout.tsx`.

```tsx
import { Toaster } from '@codedbypol/logger';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
```

### Render a toast

```tsx
import { toast } from '@codedbypol/logger';

function MyToast() {
  return <button onClick={() => toast('This is a logger toast')}>Render my toast</button>;
}
```

</Steps>
